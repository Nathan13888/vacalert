import { PhoneInputDirective } from './phone-input.directive';

describe('PhoneInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneInputDirective();
    expect(directive).toBeTruthy();
  });
});
